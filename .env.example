# Warp2Api 环境变量配置示例
# 复制此文件为 .env 并根据需要修改配置

# Warp2Api 配置
# 设置为 true 启用详细日志输出，默认 false（静默模式）
W2A_VERBOSE=false

# Bridge服务器URL配置
WARP_BRIDGE_URL=http://127.0.0.1:28888

# API Token认证 - 用于保护对外接口
# 请设置一个安全的token，不要使用默认值！
API_TOKEN=001

# ==================== Warp认证相关 ====================
# 支持多个refresh token，按优先级使用：个人token > 共享token > 匿名token

# 个人refresh token（最高优先级）
# 单个个人token - 从Warp应用中获取
WARP_REFRESH_TOKEN=your_warp_refresh_token_here

# 多个个人tokens（可选，逗号分隔）
# 如果有多个个人账号，可以在这里配置，系统会轮换使用
# WARP_PERSONAL_TOKENS=token1,token2,token3

# 共享refresh tokens（中等优先级，可选）
# 团队共享的tokens，逗号分隔
# WARP_SHARED_TOKENS=shared_token1,shared_token2

# 匿名refresh token（最低优先级，可选）
# 如果不配置，会使用内置的匿名token作为后备
# WARP_ANONYMOUS_TOKEN=anonymous_token_here

# JWT token（可选，通常会自动获取）
# WARP_JWT=your_warp_jwt_token_here

# 网络代理配置
# HTTP_PROXY=http://proxy.example.com:8080
# HTTPS_PROXY=http://proxy.example.com:8080
NO_PROXY=127.0.0.1,localhost

# ==================== 配置说明 ====================
# 1. 复制此文件为 .env：cp .env.example .env
# 2. 必须设置 API_TOKEN 为安全的token
# 3. Token优先级说明：
#    - 个人token（WARP_REFRESH_TOKEN / WARP_PERSONAL_TOKENS）：最高优先级，优先使用
#    - 共享token（WARP_SHARED_TOKENS）：中等优先级，个人token失败后使用
#    - 匿名token（WARP_ANONYMOUS_TOKEN / 内置）：最低优先级，作为后备
# 4. 多token配置示例：
#    WARP_REFRESH_TOKEN=your_personal_token
#    WARP_PERSONAL_TOKENS=token1,token2,token3
#    WARP_SHARED_TOKENS=shared1,shared2
# 5. 系统会自动轮换使用同优先级的tokens，并在失败时自动切换到下一个
# 6. 其他配置根据需要调整